linux-android-xiaomi-miatoll (4.14.336-xiaomi-miatoll-33) trixie; urgency=medium

  * Revert QG: remove hardcoded profile_judge_done (causes charger hang on
    connect; QG gauge works via voltage fallback without DS28E16 chip)
  * TAS2562: fix duplicate DT node — disable &qupv3_se4_i2c on both
    joyeuse and curtana overlays (fixes probe failure -2)
  * USB gadget: reduce transient 'failed to start g1: -19' from 8+ to ~2
    self-recovering occurrences (dwc3_gadget_start + usb_gadget_probe_driver fixes)
  * USB/charger: fix FTS gesture resume failure cascade on charger connect
  * Venus: fix video recording freeze — change video_cc_venus_ctl_axi_clk
    halt_check from BRANCH_HALT to BRANCH_HALT_DELAY (bit 31 never clears)
  * VFE: skip bus read init when bus_rd_hw_info is NULL
  * ext4: reduce container umount dmesg spam
  * debian: override KERNEL_BASE_VERSION for distinct uname -r
  * debian: set KBUILD_BUILD_USER for /proc/version tracking

 -- kilv <kilv@droidian.org>  Sat, 25 Jul 2026 00:00:00 +0800

linux-android-xiaomi-miatoll (4.14.336+usb-fix) trixie; urgency=medium

  * Build 25: silence charger/PD dmesg spam + FTS gesture resume fix + deferred USB plugin
  * smb5-lib.c: downgrade 9 pr_err/smblib_err to pr_debug/smblib_dbg (set icl current,
    start_step_vbat, battery over-voltage, dm_pulse, dp_dm, input_present, six_pin_work x3)
  * policy_engine.c: downgrade usbpd_info/pr_info to usbpd_dbg/pr_debug (tx_msgid, request pdo)
  * smb1398-charger.c: downgrade pr_err to pr_debug (curr vfloat below threshold)
  * focaltech_gesture.c: increase gesture suspend/resume retry from 5x1ms to 50x2ms;
    downgrade FTS_ERROR to FTS_INFO
  * focaltech_core.c: defer USB plugin state when TP suspended - apply directly in resume
    handler instead of dropping the event

 -- Droidian User <user@droidian.org>  Tue, 21 Jul 2026 03:40:47 +0800
