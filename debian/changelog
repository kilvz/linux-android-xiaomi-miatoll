linux-android-xiaomi-miatoll (4.14.336+droidian12+git20260709021614.7200e5b.droidian) trixie; urgency=medium

  [ Droidian User ]
  * Add Kali NetHunter support for Xiaomi miatoll (joyeuse)
  * qcacld-3.0: fix monitor mode restart by calling hdd_disable_monitor_mode on stop
  * qcacld-3.0: preserve monitor session/vdev across ifdown/ifup cycles
  * qcacld-3.0: enable monitor iface create via iw and change_iface STA→MONITOR
  * debian: update changelog for 82c30d9
  * debian/rules: add -kilv suffix to kernel version and set KBUILD_BUILD_USER=kilv
  * debian/rules: fix depmod warning in bootimage postinst
  * stop: ignore ifdown on monitor-mode adapter even via change_iface
  * mon_open: make ifup idempotent for change_iface monitor mode too
  * Revert "mon_open: make ifup idempotent for change_iface monitor mode too"
  * Revert "stop: ignore ifdown on monitor-mode adapter even via change_iface"
  * monitor: ignore ifdown/ifup cycle for change_iface-based monitor mode
  * monitor: fix PE session leak on teardown
  * change_iface: destroy STA vdev when converting to monitor mode
  * docs: add AGENTS.md with kernel build and monitor mode docs
  * change_iface: delegate monitor mode transitions to con_mode\n\nInstead of manually managing adapter vdev state in the change_iface\npath (which is fragile and crashes when the interface is DOWN before\nconversion), delegate STA<->MONITOR mode transitions to the con_mode\nmechanism. The con_mode path performs a full firmware idle\nshutdown/restart cycle, ensuring clean state initialization.\n\nThis fixes the crash that occurred when:\n1. ifdown wlan0 (STA)\n2. iw dev wlan0 set type monitor  (iff_up=false)\n3. ifup wlan0                  → delayed panic ~1s later
  * Revert change_iface con_mode delegation (deadlocks), revert __hdd_driver_mode_change to static
  * monitor: make ifdown/ifup no-ops for change_iface-based monitor mode
  * monitor: simplify ifdown no-op to avoid queue control crash
  * monitor: fix NULL deref and flush injection work on mode switch
  * debian: drop -kilv suffix from package names, update changelog
  * README: add full documentation with monitor mode fixes, usage, and build instructions
  * defconfig: disable MODULE_SIG_FORCE to allow unsigned vendor modules
  * con_mode: auto-start STA adapter when switching to MISSION_MODE
  * defconfig: tune for 24/7 Waydroid + Hermes workload
  * defconfig: BBR default, LZ4 ZRAM, Power Allocator, up-rate 2000
  * defconfig: WATCHDOG + FAIR_GROUP_SCHED
  * lib/Makefile: fix typo nmi_backgrace -> nmi_backtrace
  * defconfig: DETECT_HUNG_TASK with 30s timeout
  * defconfig: ZRAM_WRITEBACK=y
  * fix: DEFAULT_IOSCHED bfq (alldefconfig revert) + remove nosoftlockup

  [ Cédric Bellegarde ]
  * arch: arm64: configs: Add POSIX_MQUEUE

 -- Droidian User <user@droidian.org>  Thu, 09 Jul 2026 10:16:14 +0800

